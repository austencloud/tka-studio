/**
 * Prop Position Model
 *
 * Represents the calculated position for a prop - mirrors ArrowPosition structure
 */

export interface PropPosition {
  x: number; // X coordinate
  y: number; // Y coordinate
  rotation: number; // Rotation angle in degrees
}
