/**
 * Browse Type Aliases
 *
 * Type aliases and utility types for browse functionality.
 * Separated from interfaces and enums for clean architecture.
 */

// Type aliases only - interfaces belong in models/
export type FilterValue = string | number | string[] | null;
export type SortDirection = "asc" | "desc";
